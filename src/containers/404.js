import React, { Component, PropTypes } from 'react';
import { Menu, Icon, Switch, Breadcrumb, Select, QueueAnim, Dropdown } from 'antd'


const NotFoundPage = props => {
    return (<div>oops the page not found!</div>);
};

export default NotFoundPage